/**
 * Authenticated app entry via application-layer workspace root.
 * Module state is owned here and delegated to concrete modules.
 */
export { default, default as App } from "./modules/WorkspaceApp";
